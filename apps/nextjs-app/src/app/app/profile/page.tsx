import { Profile } from './_components/profile';

export const metadata = {
  title: 'Profile',
  description: 'Profile',
};

const ProfilePage = () => {
  return <Profile />;
};

export default ProfilePage;
